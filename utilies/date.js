export const currentDate = new Date();
export const dateNow = `${currentDate.getDate()}/${currentDate.getMonth() + 1}/${currentDate.getFullYear()}`;
export const houseNow = `${currentDate.getHours()}:${currentDate.getMinutes()}`;
